# Test Environment Variables
# ⚠️  WARNING: DO NOT use these values in production!
# This file is used for E2E Playwright tests with local test database only

# Next Auth
AUTH_SECRET="test-secret-key-for-e2e-testing-do-not-use-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Next Auth Discord Provider (mocked - not used in tests)
AUTH_DISCORD_ID="mock-test-discord-id"
AUTH_DISCORD_SECRET="mock-test-discord-secret"

# Prisma - Local test database
# ⚠️  Port 5433 to avoid conflicts with local PostgreSQL (production uses different connection)
DATABASE_URL="postgresql://postgres:local_test_only_password@localhost:5433/finops_test?schema=public"

# AWS KMS (mocked in tests - NOT real AWS credentials)
AWS_REGION="us-east-1"
AWS_ACCESS_KEY_ID="mock-test-access-key-id"
AWS_SECRET_ACCESS_KEY="mock-test-secret-access-key"
AWS_KMS_KEY_ID="mock-test-kms-key-id"

# Vercel Cron (test only)
CRON_SECRET="test-cron-secret-local-only"

# Resend (Email Notifications - mocked in tests)
RESEND_API_KEY="re_mock_test_key"
ADMIN_EMAIL="test@example.com"
